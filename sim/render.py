"""
Renderer — top-down RGB visualization for debugging emergence (build-spec §4.2).

Filled by the tooling fleet role (Phase 0): biome basemap + overlays for agents,
predators, structures, crop tiles, and active catastrophe events; emit png per step
and mp4 per year via imageio. Essential for sanity-checking that emergent behavior is
real (mp4 reviewed at every phase gate), not a metric artifact.
"""
